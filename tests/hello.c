// hello.exe was compiled by using:
// $ x86_64-w64-mingw32-gcc -o hello.exe hello.c
// on macOS with mingw-w64 and Wine. source code is included below:

#include <stdio.h>

int main() {
    printf("Hello from Windows PE!\n");
    return 0;
}
